[package]
name = "parser"
version = "0.1.0"
edition = "2021"

[dependencies]
log = "0.4.22"
lexer = { path = "../lexer" }
cli_common = { path = "../cli_common" }

[dev-dependencies]
pretty_assertions = "1.4.0"
